<form>
    <div class="form-group">
        <label>Untreated Injuries</label>
        <select name="injuryId">
            {{#each injuries}}
            <option value="{{this.id}}">{{this.name}}</option>
            {{/each}}
        </select>
    </div>
</form>
